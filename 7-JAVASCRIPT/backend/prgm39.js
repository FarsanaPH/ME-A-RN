// w.a.p to check a given number is mobile number or not.
mobile = "7894561230"
console.log(mobile.length == 10 ? "Its a Mobile number" : "Its not a Mobile number");

// w.a.p to check a given email is email or not.
mail = "demo@gmail.com"
console.log(mail.endsWith("@gmail.com") ? "Its a Gmail" : "Its not a gmail") ;

// w.a.p to check a given string starts with letter q
letter = "Query"
console.log(letter.startsWith("q") || letter.startsWith("Q")? "String starts with letter q" : "String starts not letter q");


